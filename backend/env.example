# Database Configuration
# Use the Connection Pooling URL from Supabase (recommended for serverless)
# Format: postgresql://postgres.[PROJECT_REF]:[PASSWORD]@[HOST]:6543/postgres
DATABASE_URL=postgresql://postgres.xxxxxxxxxxxx:YOUR_PASSWORD_HERE@aws-0-us-west-1.pooler.supabase.com:6543/postgres

# JWT Secret Key
# ⚠️  CRITICAL: Generate a secure random string: openssl rand -base64 32
# ⚠️  Must be at least 32 characters long
# ⚠️  NEVER use default values like "secret", "change-this", etc.
# Example generation: openssl rand -base64 48
JWT_SECRET=REPLACE_THIS_WITH_SECURE_RANDOM_STRING_FROM_OPENSSL

# CORS Configuration
# Comma-separated list of allowed origins (no trailing slashes)
# Development: http://localhost:3000,http://127.0.0.1:3000
# Production: https://your-domain.com,https://www.your-domain.com
ALLOWED_ORIGINS=http://localhost:3000

# GitHub OAuth (Optional)
# Get credentials from: https://github.com/settings/developers
GITHUB_CLIENT_ID=your_github_client_id_here
GITHUB_CLIENT_SECRET=your_github_client_secret_here

# Google OAuth (Optional)
# Get credentials from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id_here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Copy this file to .env and fill in your actual values:
# cp env.example .env
